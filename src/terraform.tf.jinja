terraform {
  required_version = "~> 1.9"

  required_providers {
    github = {
      source  = "integrations/github"
      version = "~> 6.0"
    }
    {% if import_existing %}
    local = {
      source  = "hashicorp/local"
      version = "~> 2.5"
    }
    {% endif %}
  }
}
